<div class="{{ class|default('') }}">
	<p class="text-font-text-black text-base font-alamain flex items-center gap-2 leading-[34px] lg:leading-[26px] lg:text-[22px]">
		{{ text }}
		{% include 'default/styled_hearth.svg' %}
	</p>
	{% if subtitle is defined %}
		<p class="text-[26px] leading-3 lg:leading-8 font-bold lg:text-[44px]">{{ subtitle }}</p>
	{% endif %}
</div>
